{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-qbe/schemas/verification_schema.json",
  "title": "AnswerVerificationOutput",
  "description": "Strict schema the independent answer-verification LLM pass must produce. See docs/PHASE0-DESIGN.md section 12 (answer verifier) / master prompt section 21.",
  "type": "object",
  "required": ["verdict", "confidence", "reason"],
  "additionalProperties": false,
  "properties": {
    "verdict": {"type": "string", "enum": ["PASS", "FAIL", "UNCERTAIN"]},
    "derived_correct_option": {
      "type": ["integer", "null"],
      "description": "0-based option index the verifier independently derived as correct."
    },
    "confidence": {"type": "number", "minimum": 0, "maximum": 1},
    "reason": {"type": "string", "minLength": 4},
    "evidence": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
